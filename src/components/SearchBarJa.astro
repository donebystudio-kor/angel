---
// Japanese-only search bar. Isolated from SearchBar.astro / SearchBarEn.astro
// — reads only from src/data/angels.ja.ts and links only to /ja/number/
// pages that actually exist. Safe to delete without touching KO/EN.
import { JA_ANGEL_NUMBERS } from "../data/angels.ja";

const numbersJson = JSON.stringify(JA_ANGEL_NUMBERS.map((a) => ({ number: a.number, summary: a.summary })));
---

<div class="search-ja-wrapper w-full max-w-md mx-auto relative">
  <form class="search-ja-form relative">
    <input
      type="text"
      name="q"
      class="search-ja-input w-full px-4 py-3 pl-10 bg-angel-card border border-angel-border rounded-xl text-angel-text placeholder:text-angel-muted/50 focus:outline-none focus:border-angel-primary focus:ring-1 focus:ring-angel-primary/30 transition-colors"
      placeholder="数字を入力(例: 111、777)"
      autocomplete="off"
      inputmode="numeric"
    />
    <span class="absolute left-3 top-1/2 -translate-y-1/2 text-angel-muted">🔍</span>
  </form>
  <div class="search-ja-results absolute top-full left-0 right-0 mt-1 bg-angel-card border border-angel-border rounded-xl overflow-hidden z-50 hidden max-h-80 overflow-y-auto"></div>
</div>

<script define:vars={{ numbersJson }}>
  const numbers = JSON.parse(numbersJson);
  const numberSet = new Set(numbers.map((n) => n.number));

  function closestAlternatives(query, limit = 3) {
    const startsWith = numbers.filter((n) => n.number.startsWith(query));
    if (startsWith.length) return startsWith.slice(0, limit);
    const contains = numbers.filter((n) => n.number.includes(query));
    if (contains.length) return contains.slice(0, limit);
    if (/^\d+$/.test(query)) {
      const q = Number(query);
      return [...numbers]
        .sort((a, b) => Math.abs(Number(a.number) - q) - Math.abs(Number(b.number) - q))
        .slice(0, limit);
    }
    return [];
  }

  function matchRowHtml(m) {
    return `<a href="/ja/number/${m.number}" class="flex items-center justify-between px-4 py-3 hover:bg-angel-border/30 transition-colors border-b border-angel-border/30 last:border-0">
      <span class="font-bold text-angel-primary">${m.number}</span>
      <span class="text-sm text-angel-muted truncate ml-3">${m.summary}</span>
    </a>`;
  }

  document.querySelectorAll(".search-ja-wrapper").forEach((wrapper) => {
    const form = wrapper.querySelector(".search-ja-form");
    const input = wrapper.querySelector(".search-ja-input");
    const results = wrapper.querySelector(".search-ja-results");

    input.addEventListener("input", () => {
      const q = input.value.trim();
      if (!q) {
        results.classList.add("hidden");
        return;
      }
      const matched = numbers
        .filter((n) => n.number.includes(q) || n.summary.toLowerCase().includes(q.toLowerCase()))
        .slice(0, 6);

      if (matched.length === 0) {
        const alternatives = closestAlternatives(q);
        results.innerHTML =
          `<div class="px-4 py-3 text-angel-muted text-sm">"${q}" のページはまだありません。</div>` +
          (alternatives.length
            ? `<div class="px-4 pb-2 text-xs text-angel-muted">近い数字:</div>` +
              alternatives.map(matchRowHtml).join("")
            : "");
      } else {
        results.innerHTML = matched.map(matchRowHtml).join("");
      }
      results.classList.remove("hidden");
    });

    form.addEventListener("submit", (e) => {
      e.preventDefault();
      const q = input.value.trim();
      if (!q) return;
      if (numberSet.has(q)) {
        window.location.href = `/ja/number/${q}`;
        return;
      }
      const alternatives = closestAlternatives(q);
      results.innerHTML =
        `<div class="px-4 py-3 text-angel-muted text-sm">"${q}" はまだ登録されていません。</div>` +
        (alternatives.length
          ? `<div class="px-4 pb-2 text-xs text-angel-muted">近い数字:</div>` +
            alternatives.map(matchRowHtml).join("")
          : `<div class="px-4 pb-3 text-xs text-angel-muted">下の一覧から探してみてください。</div>`);
      results.classList.remove("hidden");
    });

    document.addEventListener("click", (e) => {
      if (!wrapper.contains(e.target)) {
        results.classList.add("hidden");
      }
    });
  });
</script>
