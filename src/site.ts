// Single source of truth for the site's base URL. Update here only —
// astro.config.mjs, every layout, and every page that builds an absolute
// URL (canonical, og:url, hreflang, internal links) import this instead
// of hardcoding the domain.
export const SITE_URL = "https://angeldone.com";
